version: 2

sources:
  - name: DBT_SIYAPATIL008
    database: DBT_DATABASE
    schema: DBT_SIYAPATIL008

    tables:
      - name: RAW_CUSTOMERS
        config:
         freshness:
          warn_after:
            count: 2
            period: day
          error_after:
            count: 2
            period: hour
          loaded_at_field: created_at

      - name: RAW_ORDERS
        config:
         freshness:
          warn_after:
            count: 1
            period: day
          error_after:
            count: 1
            period: minute
          loaded_at_field: order_created_at

      - name: RAW_SALES
        config:
         freshness:
          warn_after:
            count: 2
            period: day
          error_after:
            count: 1
            period: day