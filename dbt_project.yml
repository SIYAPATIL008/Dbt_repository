name: 'Dbt_repository'
version: '1.0.0'
config-version: 2

profile: 'default'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
models:
    Dbt_repository:
      +materialized: incremental

vars:
  ptfe_models:
    - stg_csc_ptfe_601_plc
    - stg_aiken_ptfe_901_plc
    - stg_bnj_ptfe_701_plc
    - stg_ire_ptfe_701_plc
    - stg_demo_ptfe_801_plc